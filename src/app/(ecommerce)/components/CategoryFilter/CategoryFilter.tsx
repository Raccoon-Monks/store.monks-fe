import AsideContainer from '../Containers/AsideContainer'
import styles from './category-filter.module.css'

export default function CategoryFilter() {

    return (
        <AsideContainer id={'category-filter'} style={'category-filter-globals'}>
            FILTERS
        </AsideContainer>
    )
}